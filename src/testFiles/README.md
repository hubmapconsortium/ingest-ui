# Test Files

These files are upload fixtures used by the active Cypress bulk upload coverage.

- `Entities/Samples/Reg3Good.tsv`
- `Entities/Samples/tooMany.tsv`

Keep this directory small; prefer `cypress/fixtures/` for Cypress-only fixture
data unless the app needs a file path that matches the manual upload flow.
